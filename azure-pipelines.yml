trigger:
  - master

pool:
  vmImage: ubuntu-latest

steps:
  - task: Maven@3
    inputs:
      mavenPomFile: '$(Build.SourcesDirectory)/src/pom.xml'
      mavenOptions: '-Xmx3072m'
      javaHomeOption: 'JDKVersion'
      jdkVersionOption: '17'
      jdkArchitectureOption: 'x64'
      goals: 'clean'  # Remove 'verify' from here as you'll run tests separately
      codeCoverageToolOption: 'None'

  - task: PublishBuildArtifacts@1
    inputs:
        pathToPublish: '$(Build.SourcesDirectory)/src/target'
        artifactName: 'test-results'

  - script: |
      # Run TestNG tests using the 'testng.xml' file
      test -Dsurefire.suiteXmlFiles=$(Build.SourcesDirectory)/testng.xml
    displayName: 'Run TestNG Tests'

  - script: |
    # Debugging: List the contents of the surefire-reports directory
     ls -R $(Build.SourcesDirectory)/src
    displayName: 'List the contents of the surefire-reports directory'

  - task: PublishTestResults@2
    inputs:
      testResultsFormat: 'JUnit'  # Specify the format of your test results (e.g., 'JUnit', 'NUnit')
      testResultsFiles: '$(Build.SourcesDirectory)/target/surefire-reports/*.xml'  # Update this path to match your XML files
    displayName: 'Publish TestNG Test Results'

