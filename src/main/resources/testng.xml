<!DOCTYPE suite SYSTEM "http://testng.org/testng-1.0.dtd">
<suite name="Suite" verbose="1">

    <test name="Form Content Test">
        <parameter name="browser" value="chrome"/>

        <classes>
            <class name="Formic.testcases.FormContentTest" />
        </classes>
    </test>

    <listeners>
        <listener class-name="org.testng.reporters.JUnitXMLReporter" />
    </listeners>

    <suite-files>
        <!-- Other suite files if needed -->
    </suite-files>
    <parameter name="outputDirectory" value="src/target/surefire-reportss"/> <!-- Specify the output directory -->
</suite>

